pipeline {
  agent {
    node {
        label 'dfi-mac'
        customWorkspace '/var/jenkins-slave/workspace/game-of-life'
    }
  }
  stages {
    stage('') {
      steps {
        sh 'env'
        sh 'echo hello'
      }
    }

  }
}
